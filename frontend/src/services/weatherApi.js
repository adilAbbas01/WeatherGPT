const API_URL=import.meta.env.VITE_API_URL||"http://localhost:5000";
async function request(path){const response=await fetch(`${API_URL}${path}`);const body=await response.json();if(!response.ok||body.success===false)throw new Error(body.message||"Unable to load weather data");return body;}
export async function getWeather(city){const encoded=encodeURIComponent(city);const [current,forecast,airQuality]=await Promise.all([request(`/api/weather/current?city=${encoded}`),request(`/api/weather/forecast?city=${encoded}`),request(`/api/air-quality?city=${encoded}`)]);return {...current,forecast:forecast.forecast,temperatures:forecast.forecast.map(day=>day.temp),airQuality:{value:airQuality.aqi??"—",status:airQuality.status,description:`PM2.5: ${airQuality.pm2_5??"—"} µg/m³`},sunMoon:{sunrise:current.sunrise?new Date(current.sunrise).toLocaleTimeString([],{hour:"numeric",minute:"2-digit"}):"—",sunset:current.sunset?new Date(current.sunset).toLocaleTimeString([],{hour:"numeric",minute:"2-digit"}):"—",moonPhase:"Weather data"}};}
